jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.3-tem
  - sdk use java 17.0.3-tem
  - sdk install maven
  - mvn -v
install:
  # Build API, CORE and Paper modules
  - mvn clean install -Dmaven.javadoc.skip=true -DskipTests -pl configs-api,configs-core,configs-paper -am