jdk:
  - openjdk17
before_install:
  - sdk install java 17.0.3-tem
  - sdk use java 17.0.3-tem
  - sdk install maven
  - mvn -v
install:
  # Build only API and CORE modules (without Paper/Velocity plugins)
  - mvn clean install -Dmaven.javadoc.skip=true -DskipTests -pl configs-api,configs-core -am